# rokey_d1_ws

작업 시작 전 항상: git pull origin main

만든 프로젝트 깃허브에 올리는 법
해당 날짜 폴더로 들어가기(ex. day4)
만든 프로젝트 복사 붙여넣기(해당 폴더 안에서 만들지 말기!!!!)(프로젝트 폴더에 .git 폴더 있으면 삭제 후 업로드)
*만약 폴더가 안보인다? cp -r ~/[프로젝트] ~/rokey_d1_ws/day[날짜]

터미널에서
git add .

# 6. 커밋 및 푸시
git commit -m "fix: 서브모듈 오류 해결 및 파일 정상 업로드" (쌍따옴표 안에 설명 달기)
git push origin main
